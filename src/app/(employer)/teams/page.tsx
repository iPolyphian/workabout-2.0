import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function TeamsPage() {
  return (
    <PagePlaceholder
      title="Teams"
      description="Organise employees into teams with separate budgets"
      featureNumber={6}
    />
  );
}
