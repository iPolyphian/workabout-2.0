import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function ProviderSettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Provider account and payout settings"
      featureNumber={10}
    />
  );
}
