import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function NewPropertyPage() {
  return (
    <PagePlaceholder
      title="Add Property"
      description="List a new workspace property on Workabout"
      featureNumber={8}
    />
  );
}
